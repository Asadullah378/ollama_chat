fastapi>=0.115.0
uvicorn[standard]>=0.32.0
ollama>=0.4.0
httpx>=0.27.0
pydantic>=2.0.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
# pgvector for RAG embeddings (https://github.com/pgvector/pgvector-python).
# Requires the `vector` extension in PostgreSQL (CREATE EXTENSION vector;).
pgvector>=0.3.0
# MinerU CLI is invoked as `python -m mineru`; see https://github.com/opendatalab/mineru — you may need `pip install 'mineru[all]'` for full format support.
mineru[all]
